# Database
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Neon Database (Ejemplo)
# DATABASE_URL="postgresql://username:password@ep-xxx.us-east-2.aws.neon.tech/svportfolio?sslmode=require"

# JWT Secret (Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET="your-super-secret-jwt-key-here-change-this"

# Server Config
PORT=3000
NODE_ENV=development

# CORS - Add your frontend URLs (comma-separated)
ALLOWED_ORIGINS="https://sv-portfolio-dashboard.vercel.app,http://localhost:3000,http://127.0.0.1:3000"

# Session
SESSION_EXPIRY="7d"

# Background Jobs
ENABLE_BACKGROUND_JOBS="false"

# API Keys (Optional - Sistema funciona sin ellas usando datos locales)
# Finnhub API (Financial news) - https://finnhub.io/
FINNHUB_API_KEY=""

# Sentiment Analysis API (Optional - Falls back to local dictionary-based analysis)
# Providers: huggingface, textblob, vader
# Hugging Face: https://huggingface.co/settings/tokens (Free tier available)
# Custom API: Deploy your own sentiment API
SENTIMENT_API_KEY=""
SENTIMENT_API_PROVIDER="huggingface"
SENTIMENT_API_URL=""

# Notes:
# - SENTIMENT_API_KEY is OPTIONAL. If not provided, uses local dictionary-based analysis
# - Hugging Face provider uses FinBERT model for financial sentiment
# - Local analysis includes 100+ crypto-specific terms (moon, fud, hodl, etc.)
# - External API provides higher accuracy but requires API key
